import type { ReactNode } from "react";
import "./admin.css";

export const metadata = {
  title: "ODYSS3D — Panel administrativo",
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return children;
}